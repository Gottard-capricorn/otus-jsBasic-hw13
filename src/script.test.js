//Нужно начинать всегда с тестов ?
test("Getting a relevant response from geojs", () => {});

test("Check, that h3 element exist", () => {});
test("Check, that input element exist", () => {});
test("Check, that button element exist", () => {});

test("Check, that ul element existed correctly", () => {});

test("Check button behavior", () => {}); //Тестируем разные сценарии в одном тесте?

//тест на то, что после введения значения и нажатию кнопки получаем необходимые данные
