import { getInformation } from "./utils";
import fetchMock from "fetch-mock";

test("Getting a relevant response from geojs", () => {});

test("Check, that h3 element exist", () => {});
test("Check, that input element exist", () => {});
test("Check, that button element exist", () => {});

test("Check, that ul element existed correctly", () => {});

test("Check button behavior", () => {}); //Тестируем разные сценарии в одном тесте?
test("Verifiction data", () => {}); //тест на то, что после введения значения и нажатию кнопки получаем необходимые данные
test("empty page", () => {});
test("empty page multiple", () => {}); //множественное нажатие на клик, проверка
test("fetch", async () => {
  //   fetchMock.mockGlobal().route("https://get.geojs.io/v1/ip/geo.json", 200);
  // const getGeoResult = getInformation.getGeo();
  //   const result = await fetch("https://get.geojs.io/v1/ip/geo.json");
  // const getGeoResultLength = Object.keys(getGeoResult).length;
  // expect(typeof getGeoResult).toBe('object');
  // expect(getGeoResultLength).toBe(13);
}); //надо замокать fetch

// Понять логику работы скрипта с codeSandbox. Добавить в repomix, а потом в нейронку на обработку
// Добавить todo плагин
// Посмотреть ещё раз тот плагин анимирующий
// Использовать стиль из кирупы
